generate ci jobs:
  script:
    - "./bin/generate-gitlab-ci-yml.sh"
  tags:
    - "spack-k8s"
  image: "scottwittenburg/spack_builder_ubuntu_18.04"
  artifacts:
    paths:
      - ci-generation
    when: always
